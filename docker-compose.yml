version: "3.8"

services:
  backend:
    container_name: bsivarapp
    build: .
    ports:
      - "3100:3100"
    volumes:
      - .:/app
      - /app/node_modules
    environment:
      - CHOKIDAR_USEPOLLING=true
    command: yarn run dev